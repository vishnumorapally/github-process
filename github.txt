ls
mkdir folder_name
open that folder in vs code
cd first 
cat filename
git init
git config --global user.name "vishnumorapally"
git config --global user.email "vishnumorapally2004@gmail.com"
git add .
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/vishnumorapally/test1.git
git push -u origin main
if error go to windwos open "Control Panel\User Accounts\Credential Manager" and remove existing git hub

now we can see the file in git hub

to update the file

git add .
git commit -m "second commit"
git push -u origin main
